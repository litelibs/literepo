---
name: code-testing

on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - "**"

jobs:
  run-tests:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: format
        run: npm run format
      - name: lint
        run: npm run lint
